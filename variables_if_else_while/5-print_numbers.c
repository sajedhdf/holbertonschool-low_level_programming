#include <stdio.h>

/**
 * main - prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		putchar(x + '0');
	if (x < 9)

	}
	putchar('\n');
	return (0);
}
