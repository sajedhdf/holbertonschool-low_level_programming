#include "main.h"
#include <stdlib.h>

/**
 * _abs - Computes the absolute value of an integer
 * @n: The integer to compute the absolute value from
 *
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)

	return (-n);

	return (n);
}
